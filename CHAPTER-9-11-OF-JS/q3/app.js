var traffic_light=prompt("Enter three colors from red,green,yellow");

if(traffic_light.toLowerCase()==='red') {
    document.write("Must Stop");
}if(traffic_light.toLowerCase()==='yellow') {
    document.write("Ready to move");
}if(traffic_light.toLowerCase()==='green') {
    document.write("Move On");
}
